# tuples
# Immutable list
# cannot change once initialized

coordinates = (4, 5)
print(coordinates[0])

# If you try to assign a value to this tuple, it prompts an error

tupleArray = [(3, 5), (67, 89), (2, 5)]
tupleArray[2] = (34, 5)
print(tupleArray)

# list can modify, but tuples cannot modify
